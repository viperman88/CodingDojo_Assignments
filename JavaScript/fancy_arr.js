var names=["James", "Jill", "Jane", "Jack"];

for(var idx = 0; idx < names.length; idx++){
    console.log(idx + "->" + names[idx]);
}
    if(names.length <= 4){
        console.log(idx + "->" + names.reverse());
}
